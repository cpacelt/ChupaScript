// C API boundary — stub implementation.
//
// The public C header declares the full API surface; this translation unit
// will be filled in by Tasks 2–5. For now it only pulls in the header so the
// compiler validates the declarations and the library links cleanly.
#include "chupascript/chupascript.h"
